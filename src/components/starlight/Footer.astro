---
/**
 * Empty Footer override — Starlight renders this inside .main-pane > main,
 * but the actual footer lives in PageFrame.astro (outside .main-frame)
 * so it spans the full page width and isn't tied to individual doc pages.
 */
import type { Props } from '@astrojs/starlight/props';
---
